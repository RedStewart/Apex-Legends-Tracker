import React, { Fragment } from 'react';

const Header = () => {
  return (
    <Fragment>
      <img className='img-logo' src={require('../images/logo.png')} alt='' />
    </Fragment>
  );
};

export default Header;
